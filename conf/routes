# Routes
# This file defines all application routes (Higher priority routes first)
# https://www.playframework.com/documentation/latest/ScalaRouting
# ~~~~

# An example controller showing a sample home page
GET     /                           controllers.HomeController.index
GET     /forms                      controllers.HomeController.forms
GET     /priv                       controllers.HomeController.priv
GET     /login                      controllers.HomeController.login

POST    /formsDel                   controllers.HomeController.simpleFormPostDel
POST    /forms                      controllers.HomeController.simpleFormPost

POST    /login                      controllers.HomeController.loginPost
POST    /register                   controllers.HomeController.registerPost
POST    /logout                     controllers.HomeController.logout

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.versioned(path="/public", file: Asset)
